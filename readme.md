# Task Scheduler with Dependencies

This project implements a task scheduler that considers dependencies between tasks and visualizes execution using a Gantt chart.

## Features
- Add tasks with duration and priority
- Handle dependencies between tasks
- Detect circular dependencies
- Visualize execution order using Gantt chart

## Tech Used
- Python
- Streamlit
- Matplotlib

## How to Run
streamlit run web_app.py